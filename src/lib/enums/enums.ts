export enum tmntFormParent {  
  NEW,
  EDIT,
  RUN,  
}

export enum ioDataError {  
  NONE,
  TMNT,
  EVENTS,
  DIVS,
  SQUADS,
  LANES,
  POTS,
  BRKTS,
  ELIMS,
  OTHER_ERROR,
}

export enum ErrorCode {
  NONE,
  MISSING_DATA,
  INVALID_DATA,
  OTHER_ERROR,
}
